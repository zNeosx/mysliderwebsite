import React from 'react';
import Slider from '../components/Slider';

const Home = () => {
    return (
        <div className="Home">
            <Slider />
        </div>
    );
};

export default Home;